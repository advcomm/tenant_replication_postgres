/**
 * Database Constants
 *
 * Configuration values for database operations
 */

/**
 * Default database pool settings
 */
export const DB_POOL_MIN = 2;
export const DB_POOL_MAX = 10;

/**
 * Default database port
 */
export const DEFAULT_DB_PORT = 5432;

/**
 * Default MTDD timeout (milliseconds)
 */
export const DEFAULT_MTDD_TIMEOUT = 5000;

/**
 * Default cache TTL (seconds)
 */
export const DEFAULT_CACHE_TTL = 300;

/**
 * Default max retries
 */
export const DEFAULT_MAX_RETRIES = 3;

/**
 * PostgreSQL channel name for table changes
 */
export const TABLE_CHANGES_CHANNEL = 'table_changes';


