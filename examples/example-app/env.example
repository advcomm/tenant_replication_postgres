# Example Application Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=root
DB_NAME=mtdd_dev

# Library Configuration
PORTAL_ID=1
TENANT_COLUMN_NAME=tenant_id
TENANT_INSERT_PROC=insert_tenant
PORTAL_NAME=ExamplePortal

# gRPC Configuration (for staging/production)
# GRPC_QUERY_SERVERS=localhost:50051,localhost:50052,localhost:50053
# GRPC_LOOKUP_SERVER=localhost:50054
# GRPC_INSECURE=true

